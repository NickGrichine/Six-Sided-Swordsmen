// enum type should be upper cammel like classes
// enum values should be uppercase with underscores if needed
public enum TileType
{
    GRASSLAND,
    PURPLELAND,
    SHORE,
    OCEAN_DEEP,
    MOUNTAIN
    
}